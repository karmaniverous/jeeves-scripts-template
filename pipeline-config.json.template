{
  "_comment": "Seeded on first deploy by jeeves-tools --scope scripts. Edit this file to configure accounts, buckets, and refs. Secrets (IMAP passwords, etc.) must NOT be stored here — use the S3 secrets path and reference them at runtime. See pipeline-config.ts for full schema.",
  "accounts": [],
  "buckets": {
    "domains": [],
    "priority": []
  },
  "refs": {},
  "emailConfig": {}
}
